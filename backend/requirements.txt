# ==============================================================================
# DIPENDENZE CORE DI OMREXAMS (codice originale)
# ==============================================================================

# Elaborazione immagini e OMR
opencv-python-headless  #headless così non servono le dipendenze GUI
opencv-contrib-python-headless
scikit-image
wand                    # binding Python di ImageMagick
img2pdf
pyzbar                  # lettura QR/barcode (richiede libzbar0 nel Dockerfile)
zxing-cpp               # lettura QR alternativa (richiede libzxing nel Dockerfile)

# Generazione PDF e LaTeX
pylatex

# Gestione dati e Excel
pandas
scipy
xlrd                    # lettura file .xls (lista studenti)
openpyxl                # lettura/scrittura file .xlsx

# Database leggero (stato esami)
tinydb

# Markdown e conversione
mistletoe==0.9.0        # versione pinned: il codice originale dipende da questa API
markdownify
markdown
markdown-checklists

# Utilità varie usate dal codice originale
pyyaml                  # lettura file di configurazione .yaml
dateparser
inflect
tabulate
pypdf
click                   # CLI originale (mantenuto: potrebbe essere usato internamente)
click_log               # mantenuto per lo stesso motivo

# ==============================================================================
# DIPENDENZE DEL BACKEND WEB (nuove)
# ==============================================================================

# Framework API REST
fastapi
uvicorn[standard]       # server ASGI con extras (websockets, reload, ecc.)
pydantic                # validazione schemi (usata da FastAPI, meglio esplicitarla)
python-multipart        # necessario per upload file via FastAPI


wheel

# gestione di SSE e supporto asincrono
sse-starlette
aiofiles

